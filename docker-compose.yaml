services:
  app:
    build:
      context: .
      dockerfile: infrastructure/Dockerfile
    volumes:
      - .:/var/www/app
    working_dir: /var/www/app
    env_file: .env
    entrypoint: infrastructure/entrypoint.sh
    ports:
      - "8080:80"